import { TitleDescriptionFilterPipe } from './title-description-filter.pipe';

describe('TitleDescriptionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleDescriptionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
