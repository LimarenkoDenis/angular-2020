import { OrdinalHighlightDirective } from './ordinal-highlight.directive';

describe('OrdinalHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdinalHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
